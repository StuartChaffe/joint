jQuery(document).ready(function($) {

	$(".gallery").slick({
		// settings: "unslick",
		// speed: 200,
		// slidesToShow: 1,
		// slidesToScroll: 1,
		// autoplay: true,
		// autoplaySpeed: 5000,
		// arrows: false,
		// infinite: true,
		// dots: true,
		// adaptiveHeight: true,

		dots: true,
		arrows: false,
		infinite: true,
		speed: 600,
		slidesToShow: 1,
		adaptiveHeight: true
	});  
});